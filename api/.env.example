# Environment: 'dev' (uses proxy) or 'prod' (direct connection)
ENVIRONMENT=dev

# ScyllaDB Connection Settings (for production/direct connection)
SCYLLA_HOST=localhost
SCYLLA_PORT=9042
SCYLLA_CONNECT_TIMEOUT=10.0

# Proxy Configuration (for development with remote ScyllaDB)
# Default proxy host and port
SCYLLA_PROXY_DEFAULT_HOST=your-proxy-host.example.com
SCYLLA_PROXY_DEFAULT_PORT=9042

# Proxy mapping: maps internal ScyllaDB node IPs to public proxy endpoints
# Format: JSON object with "internal_ip": ["proxy_host", proxy_port]
SCYLLA_PROXY_MAPPING={"172.27.0.2": ["your-proxy-host.example.com", 9037], "172.27.0.3": ["your-proxy-host.example.com", 9038], "172.27.0.5": ["your-proxy-host.example.com", 9039]}

# API Authentication
# Comma-separated list of valid API keys (leave empty to disable auth in dev)
API_KEYS=dev-key-replace-in-production
